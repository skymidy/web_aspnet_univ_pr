export type ProfileInput = {
  knowledge: string;
  interests: string;
  searchStatus: boolean;
  mentorSearchStatus: boolean;
  mentorStatus: boolean;
  description: string;
};

export const convertToProfileInput = (data: unknown) => {
    
}